
@model EditorViewModel

@{
    string fieldID = "f_" + Guid.NewGuid().ToString("N");
}
<span id="@fieldID" data-field="@Model.PropertyName"></span>
@*<span class="glyphicon glyphicon-coins">&nbsp;</span><span id="@fieldID"></span>*@
<script>
    $(function () {
        var $f = $("#@fieldID");

        $f.closest("form").on("onAfterBind", function (e, form) {
            if ($f.closest("form")[0].id != form.element[0].id) {
                return;
            }
            if (form.getPr("@Model.PropertyName")) {
                $f.html(kendo.toString(form.getPr("@Model.PropertyName"), "n0"));
            } else {
                $f.html("");
            }
        });
    });
</script>


